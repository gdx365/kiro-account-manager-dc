# KAMRS 整改进展（2026-05-06）

## 已完成

1. 前后端命令契约修复
- 修复 CLI 切号参数链路，移除前端对不存在命令的调用。
- 新增命令契约检查脚本并接入 npm scripts：
  - `check:commands`
  - `check:deprecated`
  - `report:commands`

2. `machine_guid` 历史残留命令清理
- 已移除并注销以下未使用命令：
  - `backup_machine_guid`
  - `restore_machine_guid`
  - `get_machine_guid_backup`
  - `clear_macos_override`
  - `restart_as_admin`
- 保留且仍在使用：
  - `get_system_machine_guid`
  - `reset_system_machine_guid`
  - `set_custom_machine_guid`
  - `generate_machine_guid`

3. 账号-机器码绑定模式下线
- 已移除前后端 `bind_machine_id_to_account` 配置与 UI 分支。
- 切号逻辑统一为：开启 `autoChangeMachineId` 时使用随机机器码切换。

4. 构建与配置修复
- `vite.config.js` 迁移为 `vite.config.mjs`。
- `package.json` 脚本和构建链路修复。
- `.gitignore` 放开 `docs/` 与 `scripts/`，整改文档与检查脚本可纳入版本控制。

5. 乱码与文本修复（本轮）
- 修复了关键路径中的语法损坏和部分乱码文本（含 `main.rs`、`app_settings_cmd.rs`、`index.html`、`tauri.conf.json` 等）。
- 编码扫描脚本可用：`check:encoding`。

## 未完成

1. 全仓历史乱码仍未彻底清零
- 仍存在大量“可读性差但不影响编译”的历史注释/文案（分散在 Rust/前端/文档）。
- 当前策略是“优先修运行链路 + 增量修乱码”，未做一次性全文重写。

2. 命令矩阵候选清理未逐项落地
- `docs/命令矩阵报告.md` 中“已注册但前端未直接调用”的命令仍较多。
- 需继续逐项标记为 `active/internal/deprecated` 并安排下线节奏。

3. 文档与注释语义统一仍需继续
- 多份历史文档的结论已过时，需持续按代码现状更新。

## 本轮验证结果

- `cargo check` 通过
- `cargo test` 通过（157 tests passed）
- `npm run build` 通过
- `npm run check:commands` 通过
- `npm run check:deprecated` 通过
- `npm run check:encoding` 通过

## 下一步建议

1. 基于最新命令矩阵，继续做“低风险命令裁剪”批次。
2. 针对 `locales/` 与关键 Rust 注释，做第二轮定向乱码清理。
3. 每完成一批裁剪即跑全量回归并更新文档。
