# KAMRS 整改进展（2026-05-06）

## 已完成

1. 前后端命令契约修复
- 修复 CLI 切号调用参数不一致问题。
- 增加并接入检查脚本：
  - `npm run check:commands`
  - `npm run check:deprecated`
  - `npm run report:commands`

2. `machine_guid` 历史残留清理
- 已移除命令：
  - `backup_machine_guid`
  - `restore_machine_guid`
  - `get_machine_guid_backup`
  - `clear_macos_override`
  - `restart_as_admin`
- 仍保留并在用：
  - `get_system_machine_guid`
  - `reset_system_machine_guid`
  - `set_custom_machine_guid`
  - `generate_machine_guid`

3. 账号机器码绑定模式下线
- 已移除 `bind_machine_id_to_account` 前后端配置与 UI 分支。
- 切号逻辑统一为：`autoChangeMachineId=true` 时使用随机机器码。

4. 构建与工程收口
- `vite.config.js` 迁移为 `vite.config.mjs`。
- 修复 `package.json` 脚本与构建链路。
- `.gitignore` 放开 `docs/` 与 `scripts/`。

5. 网关模型兜底策略
- 当请求未知模型时，统一回退到 `claude-sonnet-4.5`。

## 未完成

1. 全仓历史乱码未清零
- 仍有历史注释/文案存在可读性问题，需要继续分批清理。

2. 命令矩阵收口未完成
- “已注册但前端未直接调用”的命令仍较多，需继续做分组标记和逐批裁剪。

3. 文档持续同步
- 历史文档中仍有部分结论滞后于当前代码，需要继续更新。

## 当前验证基线

- `cargo check` 通过
- `cargo test` 通过（157 passed）
- `npm run build` 通过
- `npm run check:commands` 通过
- `npm run check:deprecated` 通过
- `npm run check:encoding` 通过
